export type DebugOnly = {
    showCreateSourceCode: boolean; // show button to view source code of the new manifest editor
};

export const initialDebugOnly: DebugOnly = {
    showCreateSourceCode: false,
};
