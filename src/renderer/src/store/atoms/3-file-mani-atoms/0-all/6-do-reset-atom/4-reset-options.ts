import { type ResetManifestDataParams } from "./9-types";
import { type ManiOptions } from "../../4-options";

export function resetFormOptions(optionsAtoms: ManiOptions.FormOptionsAtoms, restParams: ResetManifestDataParams) {
    // const { newMani, get, set } = restParams;

    // const { fields, submittype } = formCtx;

    // const newFields = fields.map((field) => {
    //     return {
    //         ...field,
    //         value: field.default,
    //     };
    // });

    // return {
    //     newFields,
    //     submittype,
    // };
}